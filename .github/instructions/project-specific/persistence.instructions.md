---
description : Guidelines for how persistance project is structured in line with modular monolith architecture
applyTo: '*MyHomeRamen.Persistance*.cs'
---

# Persistence Instructions

## Overview
Persistence layer (`MyHomeRamen.Persistance`) handles database operations using EF Core by implementing abstractions defined within `MyHomeRamen.Domain` project.

## Guidelines
- Use `DbContext` for entities and relationships.
- Apply query filters for multi-tenancy (e.g., restaurant ID).
- Apply AsNoTracking for read-only queries to improve performance.
- IDs will be generated by appication layer so no need to configure ID generation in EF Core.
- Repository pattern should be used only for specific scenarios e.g. caching
- Simple operations should use proper DBContext abstractions (e.g. `IOrdersDbContext`) and specification extenstions.
- Each module's `DbContext` implementation should inherit from `BaseDbContext` which contains common configurations and logic for all contexts (e.g. multi-tenancy, soft deletes, etc.)

## DB Specifications & Extensions
- All custom database queries, existence checks, and uniqueness checks MUST be implemented as generic or specific extension methods extending `IQueryable<T>` or specific `DbSet` interfaces.
- Place these in `MyHomeRamen.Persistance.Common.DbExtensions`.
- Never write inline `AnyAsync()` or `FirstOrDefaultAsync()` with complex business rules in handlers or validators; abstract them into an extension method (e.g., `_dbContext.Products.IsNameUniqueAsync(name, cancellationToken)`).
- EF Core Value Converters (e.g. `CategoryIdConverter`) automatically handle strong ID boxing, so DO NOT manually unbox IDs in LINQ Expressions.

## Structure
- Each module has its own `DbContext` interface defined in `MyHomeRamen.Domain` project and implementation in the `Persistence` project.
- Each module has its own folder in the `Persistance` project which contains
	- `DbContext` implementation class
	- Migrations folder for EF Core migrations related to the module
	- Configurations folder with `IEntityTypeConfiguration` implementations for the module's entities
	- Converters folder for any custom value converters needed for the module's entities
	
## Example structure
MyHomeRamen.Persistance/
|-- Common/
|	-- Specifications/
|	|	-- GetSpecificationExtensions.cs
|	|	-- UpdateSpecificationExtensions.cs
|	|	-- DeleteSpecificationExtensions.cs
|-- Orders/
|   |-- Configurations/
|   |-- Converters/
|   |-- Migrations/
|   |-- OrdersDbContext.cs
